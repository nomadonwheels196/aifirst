# Step 50 — Reduce Summary (Draft)

You are given multiple per‑chunk summaries (bullets + narrative). Produce a single cohesive JSON summary.

Output JSON ONLY (no prose). Use EXACTLY one top-level key: summary.
{
  "summary": {
    "bullets": ["..."],
    "narrative": "..."
  }
}
