# Step 40 — Summary (Draft)

Create an executive summary (5–8 bullets) and a one‑paragraph narrative, with explicit assumptions and any confidence caveats.

Output JSON ONLY (no prose). Use EXACTLY one top-level key: summary. Do not add any other keys.
{ "summary": { "bullets": ["..."], "narrative": "..." } }
